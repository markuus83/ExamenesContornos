```Shell

    $ mkdir e2

    $ cd e2

    $ git add e2.md

    $ git commit -m "Añadido fichero e2.md"

    $ nano .gitignore

        (Escribimos '*.class' dentro del archivo .gitignore)
    
    $ nano HolaMundo.java

    $ javac HolaMundo.java

    $ git add .

    $ git commit -m "Acabada parte de git terminal en el examen"

    $ git push 

```